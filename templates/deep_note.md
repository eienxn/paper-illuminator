# {{TITLE}}

> **Authors:** {{AUTHORS}}
> **Affiliations:** {{AFFILIATIONS}}
> **Paper:** {{PAPER_URL}} ({{DATE}})
> **Code:** {{REPO_URL}} (or `not found`)
> **Reading depth:** {{DEPTH}} · **Figures:** {{FIG}} · **Flow style:** {{FLOW_STYLE}} · **Language:** {{LANG}}
> **Note generated:** {{TODAY}}

> {{ONE_LINE_PITCH}}  ← anchor for the whole document, ~1 sentence
>
> After reading this you should be able to:
> - {{LEARNING_GOAL_1}}
> - {{LEARNING_GOAL_2}}
> - {{LEARNING_GOAL_3}}
>
> All visualizations are in `figures/`, generated by `.py` scripts so they can be re-run.

> **Anchor concept** (the foundational fact the paper rests on, stated upfront so every section can refer back):
> **{{ANCHOR_CONCEPT}}**

---

<!--
AGENT NOTE (private — do NOT render this comment block to the reader output):

Before writing, identify Step A domains (see references/mining_playbooks.md):
- ${DOMAIN_LIST}  (e.g., NN method + iterative algorithm)
- Loaded playbooks: ${LOADED_PLAYBOOKS}
- Skipped playbooks: ${SKIPPED_PLAYBOOKS}

This is your private planning. The reader doesn't care which mining playbooks
you loaded — they want to learn the paper. Apply the playbooks silently.

If you need to be transparent about your reading choices, do it in Appendix A
(Paper figure / table audit) — that is reader-facing transparency about which
paper figures you deep-read, not about the skill's internal mechanism.
-->

## Table of Contents

- **Preface** — Why this paper matters + anchor concept
- **§1 Background** (prerequisite concepts, each Lego-first)
  - 1.1 An intuition (analogy)
  - 1.2 ~ 1.N {{BACKGROUND_TOPICS}}
- **§2 Core structure of {{METHOD_NAME}}**
  - 2.0 Motivation for the load-bearing design choices (≥2 walkthroughs, each ≥4 of the 7 motivation moves)
  - 2.1 One-figure overview
  - 2.N Components
- **§3 Training details** (if applicable)
- **§4 Inference / execution details** (with concrete worked example)
- **§5 Evaluation and findings**
- **§6 Counter-intuitive points** (dedicated chapter)
- **§7 Pitfalls / reproduction details**
- **§8 Assessment** (only when `--context` was provided)
- **Appendix A** — Paper figure / table audit (transparency: which paper figs read vs skipped)
- **Appendix B** — Glossary
- **Appendix C** — Claim ledger (evidence source + confidence + caveat)
- **Appendix D** — Summary card (10 fields)
- **Appendix E** — Self-iteration log (only present if `--iter ≥ 2`)

---

# Preface: why this paper matters

{{INTRO_PROSE}}

**Anchor concept**: {{ANCHOR_CONCEPT_STATEMENT}}  [evidence: e.g., interpretation / paper-reported §X]

How this anchor runs through the document:

| Section | How the anchor appears |
|---|---|
| §2.X | {{}} |
| §3.X | {{}} |
| §5.X | {{}} |

---

# §1 Background

## 1.1 An intuition

{{ANALOGY_AND_LEGO_TOY}}

## 1.2 ~ 1.N {{Prerequisite concepts}}

One subsection per non-trivial prerequisite. Each goes: analogy → formula → simple Lego worked example → **figure (mandatory: at least one of geometric intuition / concept-relationship map / analogy figure / neighboring-concept side-by-side / "where this lives in the paper" anchor diagram)** → code (if useful).

Per `figure_contract.md` §2, **each non-trivial §1 prerequisite gets at least one background concept illustration** — this is separate from the worked-example figure and provides non-numerical intuition. Skipping these is the most common reason a deep note feels "patient on numbers but cold on intuition".

### Subsection template

#### 1.N {{prerequisite name}}

**Analogy**: {{everyday or domain-adjacent analogy}}

**Formula / definition**: {{spelled out with symbol meanings}}

**Geometric / structural intuition figure**:

![{{prerequisite}} intuition](figures/NN_bg_{{slug}}.png)

**[provenance tag]** Reading: {{what the figure shows / key elements / what to take away}}

**Lego worked example** (if numerical):

```
{{small concrete computation, B=2 / N=5 scale}}
```

**Where this lives in the paper**: {{which sections / which equations the reader will encounter this in later}}

---

# §2 Core structure of {{METHOD_NAME}}

## 2.0 Motivation for the load-bearing design choices

> Per `teaching_moves.md` #9 — pick the 2–4 most load-bearing design choices the paper made (the regularizer form, the loss family, the data-split rule, the architecture cardinality, the proof strategy, the chosen estimator, etc.) and walk each one through **before** explaining how it works in §2.1+. Each walkthrough uses **≥4 of the 7 motivation moves**.
>
> The mining-side raw material comes from `mining_playbooks.md` Step B "Design decision audit" — alternatives, failure mechanisms, comparison numbers, common misconceptions.

### 2.0.1 Why {{DESIGN_CHOICE_1}}?

**Common misconception to defuse first** _(move #3)_: {{MISCONCEPTION_AND_CORRECTION}}

**Reasons** _(move #2 — number them if multiple)_:
- ① {{REASON_1_WITH_INTUITION_AND_PAYOFF}}
- ② {{REASON_2}}
- ③ {{REASON_3}}

**What the alternative does wrong, mechanically** _(move #4)_: {{ALTERNATIVE_FAILURE_MECHANISM — "at scale Z, the alternative does X bad thing through mechanism Y"}}

**3-panel comparison figure** _(move #5)_:

![{{DESIGN_CHOICE_1}} motivation](figures/NN_motivation_{{slug}}.png)

**[provenance tag]** Panel-by-panel reading:
- **Left**: ground-truth structure ({{what the real signal looks like}})
- **Middle**: what the alternative does to it ({{specific distortion}})
- **Right**: what {{DESIGN_CHOICE_1}} does ({{how it preserves the signal}})

**Numerical comparison** _(move #6)_:

| Option | {{Metric}} | |
|---|---|---|
| **{{chosen}}** | **{{number}}** | ← chosen |
| {{alternative 1}} | {{number}} | |
| {{alternative 2}} | {{number}} | |

**Self-rebuttal** _(move #7)_:

> **Sharp objection a sceptical reader would raise**: "{{SHARP_OBJECTION}}"
>
> **Honest answer**: {{Not-defensive answer — concede what is true, then explain why the design still holds}}.

**Evidence**: {{[paper-reported, §X] / [interpretation] / [synthetic-demo for the comparison figure]}}

### 2.0.2 Why {{DESIGN_CHOICE_2}}?

(Same structure — pick ≥4 of the 7 moves that fit.)

---

## 2.1 One-figure overview

![{{METHOD_NAME}} end-to-end overview](figures/01_overview.png)

**[provenance tag]** Panel-by-panel reading:
- {{}}
- {{}}

## 2.N Components

One subsection per component, with shape annotations for ML (e.g. `(B, T, D)`) / numbered proof steps for theory / numbered protocol steps for empirical work — whatever fits the domain.

---

# §3 Training details (if applicable)

How the data is sliced / what the loss is / key hyperparameters / parameter-count breakdown.

Include a **worked example with simple Lego numbers** (`B=2, L=3, D=4` or analogous toy scale) before any paper-scale numbers.

---

# §4 Inference / execution details (if applicable)

One-step end-to-end walkthrough. Must include a **concrete numerical example** (one full algorithm iteration / one proof step / one protocol step) with actual numbers.

## 4.0 Running example (carries through §4.1 – §4.N)

> Per `teaching_moves.md` #11 — declare **one** concrete example here and reuse the **same numbers** through every stage below. Required whenever the paper has a pipeline (training / inference / iterative algorithm / multi-step protocol / proof chain). For training pipelines, this same block belongs in §3.0 instead.

**Running example.** ${one-line setup of the concrete inputs and small hyperparameters}:
- Input: ${e.g., observation o = 2×2 image [[0.8, 0.2], [0.1, 0.9]]}
- Hyperparameters (toy scale): ${e.g., K=4, H=3, batch=2, dim=2}
- Goal / target: ${e.g., z_goal = (1.0, 0.5)}

![{{METHOD_NAME}} pipeline with running-example values annotated at each stage](figures/NN_running_example.png)

**[provenance tag]** Stage-by-stage walkthrough using the same numbers:

### Stage 1 — ${first stage name}
- **Input**: ${value from running example}
- **Operation**: ${formula, abbreviated}
- **Output**: ${value}
- **Interpretation**: ${one line on what changed and why}

### Stage 2 — ${second stage name}
- **Input**: ${output of Stage 1 — same value}
- **Operation**: ${...}
- **Output**: ${value}
- **Interpretation**: ${...}

### Stage N — ${final stage name}
- **Input**: ${same chain}
- **Operation**: ${...}
- **Output**: ${value}
- **Interpretation**: ${...}

**Closing**: you just saw a complete ${pipeline-name} run with concrete numbers. The paper actually uses ${paper-scale numbers, e.g., K=512, H=15, dim=192} — every step is mechanically identical, just at larger scale.

[Evidence: synthetic-demo / reproduced if you ran the released code with these inputs]

---

## 4.N Stage-specific deep dives (optional)

If a particular stage of the running example needs its own deep treatment (counter-intuitive behavior, hidden complexity, etc.), expand it in a sub-section here — **but keep referencing the running example values**, do not introduce new ones.

---

# §5 Evaluation and findings

Main results / compute cost / key ablations. Every number tagged with its evidence source.

---

# §6 Counter-intuitive points (dedicated chapter)

Each item uses a three-act structure:

### Counter-intuitive N: "You might think X"
- **You might think**: {{naive view}}
- **But actually**: {{actual answer}}
- **Why naive is wrong**: {{mechanism}}
- **Worked example showing the gap**: {{concrete computation}}
- **Evidence**: [paper-reported, §X] / [interpretation]

---

# §7 Pitfalls / reproduction details

Each item uses "phenomenon → where it triggers → fix" structure.

---

# §8 Assessment

> ⚠️ **Fill this section ONLY if `--context` was provided.** §1–§7 stay project-agnostic — no lab / survey / "should we include this in our review" / follow-up-plan language anywhere in §1–§7.
>
> This is the only section where reader-specific context belongs.

{{CONTEXT_SPECIFIC_ASSESSMENT}}

---

# Appendix A · Paper figure / table audit

> Transparency: list before generating custom figures, so reader (and your future self) can trace your functional choices.

**Paper figure inventory**:
- Fig 1: {{purpose}} → {{what I did: deep-read / one-line mention / skip}}
- Fig 2: ...
- ...

**Paper table inventory**:
- Tab 1: {{purpose}} → {{what I did}}
- ...

**My custom figures (figures/) inventory**:
- 01_xxx.png: {{teaching gap this fills}}  [synthetic-demo / interpretation redraw]
- ...

---

# Appendix B · Glossary

(See mining_playbooks.md for the principle: every acronym used in §1–§7 must have a first-mention definition OR appear here.)

## Acronyms

| Acronym | Full name | One-line gloss |
|---|---|---|
| {{}} | {{}} | {{}} |

## Proper nouns

| Term | Definition |
|---|---|

## Notation

| Symbol | Meaning | Shape / type |
|---|---|---|

## Neighboring concepts (commonly confused)

| Don't confuse | A | B | Key difference |
|---|---|---|---|

---

# Appendix C · Claim ledger

> All load-bearing claims, with 5-column traceability.

| Claim | Evidence | Source type | Confidence | Caveat |
|---|---|---|---|---|
| {{}} | {{section / fig / tab / repo path}} | paper-reported / repo-inspected / reproduced / synthetic-demo / interpretation | strong / medium / weak | {{}} |
| ... | | | | |

---

# Appendix D · Summary card (10 fields)

Quick reference. Full reasoning lives in §1–§7.

1. **One-line core**: {{}}
2. **Question it answers**: {{}}
3. **The method claimed**: {{}}
4. **Main claims** (tagged [strong/medium/weak]): {{}}
5. **Evidence for each claim + how much I believe it**: {{}}
6. **Main baselines + datasets** (if applicable): {{}}
7. **Compute / resource needs**: {{}}
8. **Reproduction difficulty** (repo / license / README quality 1–5 / overall 1–5): {{}}
9. **Counter-intuitive points** (one-liner list, see §6 for full version): {{}}
10. **Limitations / failure modes** (paper-acknowledged + ones you spotted): {{}}

---

{{IF_ITER_GE_2}}

# Appendix E · Self-iteration log

> Filled when `--iter ≥ 2`. Transparent record of how the note was revised by the first-pass-reader viewpoint passes (per SKILL.md Step 6.5). Lets the human reader trust that self-review happened, and surfaces where typical friction points were.

## Iteration 2 — first self-review pass

**Friction points found**: {{N_FRICTION_POINTS_ITER2}}

| Location | What the first-pass reader stumbled on | Fix applied |
|---|---|---|
| §X.Y | "Wait, what is M here?" | Added step A of toolchain unfurling — M is now defined as ${concrete object} before SVD is applied. |
| §X.Y | "Why SVD instead of EVD?" | Added step C — one-paragraph refresher + 'why SVD' justification. |
| §X.Y | "I have to believe this on faith." | Added `[paper-reported, Tab N]` tag and the exact number. |
| ... | ... | ... |

(If 0 friction points were found in this pass, write "0 friction points — note passes first-pass-reader test from this iteration on" and stop. Do not invent fixes.)

{{IF_ITER_GE_3}}

## Iteration 3 — second self-review pass

**Friction points found**: {{N_FRICTION_POINTS_ITER3}}

| Location | What the first-pass reader stumbled on | Fix applied |
|---|---|---|
| ... | ... | ... |

{{ENDIF}}

{{IF_ITER_GE_4}}

## Iteration 4 — third self-review pass (cap)

**Friction points found**: {{N_FRICTION_POINTS_ITER4}}

| Location | What the first-pass reader stumbled on | Fix applied |
|---|---|---|
| ... | ... | ... |

{{ENDIF}}

{{ENDIF}}

---

{{IF_CONTEXT_PROVIDED}}

## suggested-survey-paragraph

> ⚠️ Draft for your review. Verify facts and wording before pasting into any external document.

{{SURVEY_DRAFT}}

{{ENDIF}}

{{IF_RELATED}}

## Related local notes
- [{{other-slug}}](../{{other-slug}}/{{title}}详解.md) — {{one-line relationship}}

{{ENDIF}}

---

<!--
AGENT NOTE (private — do NOT render this comment block to the reader output):

After writing, run this self-check before declaring done. If any item fails, fix it.
The reader never sees this checklist — it is QA, not content.

- [ ] Anchor concept is in the preface, with the "where this appears" table filled
- [ ] §1 has at least 4 prerequisite subsections
- [ ] Every worked example is Lego-first (B=2 / N=5 scale) before paper-scale
- [ ] §2.0 has ≥ 2 motivation walkthroughs (one per top design choice), each using ≥ 4 of the 7 motivation moves; each sits **before** the corresponding method exposition
- [ ] Each motivation paragraph passes the **circularity test**: if you replace the method name with "X", it still contains specific facts the reader couldn't have guessed (not just "we use X because we want X")
- [ ] Every math tool or prerequisite construct introduced (SVD, KL, Fisher, "the matrix M", etc.) completes the 4-step toolchain unfurling (#10) — materialize the object / state what you want / refresh the tool + justify why this tool / compute on a 2×2 toy — **before** the tool is used downstream
- [ ] Passes the **fait accompli test**: read every tool introduction as a first-time reader — if you find yourself wanting to ask "wait, what *is* M?" or "wait, *why* this tool?", step A/B/C is missing
- [ ] If `--iter ≥ 2`, ran the self-iteration loop (per SKILL.md Step 6.5): switched to first-pass-reader viewpoint, marked friction points, edited the body in-place, logged each fix into Appendix E
- [ ] **iter=1 alone meets the no-assumed-knowledge baseline.** No passage relies on "they probably know this" or "iter=2 will catch it". If you removed Step 6.5 entirely, the note would still pass the first-pass-reader test
- [ ] If the paper has any end-to-end flow (training / inference / iterative algorithm / multi-step protocol / proof chain), §3.0 or §4.0 has a **running example** with the same concrete numbers carried through every stage, plus a stage-by-stage flow figure. Numbers do not silently switch between stages
- [ ] ≥ 5 counter-intuitive points in §6, each in three-act format
- [ ] ≥ 3 pitfalls in §7
- [ ] **§1–§7 has no lab / survey / "should we include" / context-specific language** (context firewall)
- [ ] Appendix C claim ledger filled with ≥ 5 load-bearing claims
- [ ] Every non-trivial number traces back to a tag (paper-reported / repo-inspected / reproduced / synthetic-demo / interpretation)
- [ ] No "demonstrates" / "proves" / "shows" / "实测" / "证明" without a clear source
- [ ] Appendix A paper figure/table audit is filled
- [ ] Figure count meets figure_contract.md §2 functional budget (paper ≥4 / worked ≥3 / architecture ≥3 / results ≥3 / real evidence ≥1 if feasible / motivation comparison ≥2 / toolchain toy ≥2 / running-example flow ≥1 when pipeline exists / **§1 background illustration ≥3, with at least one figure per non-trivial §1 prerequisite**). Floor: 23 (or 22 without pipeline). Cap: 35. Sweet spot: 25–30
- [ ] Every non-trivial §1 background prerequisite has at least one intuition figure (geometric / concept-relationship / analogy / neighboring-concept). §1 text-only with no figures fails the deep-mode pedagogical bar
- [ ] Every figure caption leads with a provenance tag, followed by prose panel-by-panel reading
- [ ] **No agent meta-content (Step A domain identification, self-check, "which playbooks I loaded") rendered into the reader output**
- [ ] If `--lang` was passed, all reader-facing prose is in the requested language; if not, default to English
-->
